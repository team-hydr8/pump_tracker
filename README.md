# pump_tracker

## Running the program:
```sh
pip install -r requirements.txt
python main.py
```

### If using venv:
```sh
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```